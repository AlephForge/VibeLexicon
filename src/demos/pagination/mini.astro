---
// Mini Preview: pagination（分页）-- 纯静态，零 JS，Gallery/Stream 用
---
<div class="w-full aspect-[16/9] bg-canvas border border-line rounded-card overflow-hidden p-4 flex flex-col gap-2">
  <div class="flex-1 grid grid-cols-3 gap-2">
    <div class="rounded-control bg-card border border-line p-2 flex-1"></div>
    <div class="rounded-control bg-card border border-line p-2 flex-1"></div>
    <div class="rounded-control bg-card border border-line p-2 flex-1"></div>
  </div>
  <div class="flex items-center justify-center gap-1.5 flex-none">
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">‹</span>
    <span class="w-6 h-6 rounded-control bg-dark-bg text-white flex items-center justify-center text-[10px]">1</span>
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">2</span>
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">3</span>
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">…</span>
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">8</span>
    <span class="w-6 h-6 rounded-control border border-line flex items-center justify-center text-[10px] text-muted">›</span>
  </div>
</div>